# Tier 1 LLM — Groq (primary, fast inference)
# Get from: console.groq.com → API Keys → Create
GROQ_API_KEY=gsk_xxxxx
GROQ_API_KEY_2=gsk_xxxxx
LLM_MODEL=llama-3.3-70b-versatile

# Tier 2 LLM — Gemini (fallback, different vendor = uncorrelated failure)
# Get from: aistudio.google.com → Get API Key
GEMINI_API_KEY=AIzaSy_xxxxx
GEMINI_MODEL=gemini-2.5-flash

# Deployment (filled in after Phase 6 Render deploy)
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com
